---
phase: 02-merge
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - tests/test_merge.py
  - run_merge.py
autonomous: true
requirements:
  - MERGE-01
  - MERGE-02
  - MERGE-03
  - MERGE-04

must_haves:
  truths:
    - "python -m pytest tests/test_merge.py -x -q exits 0 with all tests passing"
    - "TestRnaseqPivot verifies that only top-N genes (by variance) appear as wide columns"
    - "TestCnvPivot verifies that arm columns are named with pattern 1p/1q (chr prefix stripped, arm letter appended)"
    - "TestMethylationPivot verifies null beta_values are handled correctly by mean aggregation"
    - "TestMergeAllCohorts verifies inner-join semantics: patients missing from any modality are excluded"
    - "TestMergeAllCohorts verifies one row per patient in the merged output"
    - "run_merge.py calls merge_all_cohorts(DATA_DIR, OUTPUT_DIR) and prints the output path"

  artifacts:
    - path: "tests/test_merge.py"
      provides: "Unit tests for all four MERGE-XX requirements"
      contains: "TestRnaseqPivot, TestCnvPivot, TestMethylationPivot, TestMergeAllCohorts"
    - path: "run_merge.py"
      provides: "Minimal orchestrator runner — mirrors run_pipeline.py"
      contains: "merge_all_cohorts"

  key_links:
    - from: "tests/test_merge.py"
      to: "src/merge.py"
      via: "deferred import inside each test method: from src.merge import merge_all_cohorts"
      pattern: "from src\\.merge import"
    - from: "run_merge.py"
      to: "src/merge.py"
      via: "direct import and call: merge_all_cohorts(DATA_DIR, OUTPUT_DIR)"
      pattern: "merge_all_cohorts"
---

<objective>
Write tests/test_merge.py covering all four MERGE-XX requirements and create run_merge.py as the merge pipeline entry point.

Purpose: Prove the Phase 2 transform pipeline is correct and provide a runnable script that mirrors the run_pipeline.py pattern from Phase 1.

Output:
  - tests/test_merge.py — four test classes (one per MERGE-XX req), synthetic Parquet helper, no mocking needed
  - run_merge.py — minimal runner (no argparse, no __main__ guard) calling merge_all_cohorts(DATA_DIR, OUTPUT_DIR)
</objective>

<execution_context>
@/Users/aidancarlisle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aidancarlisle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/aidancarlisle/Documents/cancer_genome_atlas_research/.planning/ROADMAP.md
@/Users/aidancarlisle/Documents/cancer_genome_atlas_research/.planning/phases/02-merge/02-RESEARCH.md
@/Users/aidancarlisle/Documents/cancer_genome_atlas_research/.planning/phases/02-merge/02-PATTERNS.md
@/Users/aidancarlisle/Documents/cancer_genome_atlas_research/.planning/phases/02-merge/02-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from merge.py (created in plan 02-01). -->

From src/merge.py (plan 02-01 output):
```python
COHORTS = ["TCGA-BRCA", "TCGA-LUAD", "TCGA-PRAD"]
N_RNA_GENES = 500
N_METH_PROBES = 500

def _top_n_by_variance(parquet_paths: list[pathlib.Path], id_col: str, value_col: str, n: int = 500) -> list[str]: ...
def _pivot_rnaseq(parquet_path: pathlib.Path, top_genes: list[str]) -> pl.DataFrame: ...
def _pivot_cnv(parquet_path: pathlib.Path) -> pl.DataFrame: ...
def _pivot_methylation(parquet_path: pathlib.Path, top_probes: list[str]) -> pl.DataFrame: ...
def merge_all_cohorts(data_dir: pathlib.Path, output_dir: pathlib.Path) -> pathlib.Path: ...
```

From run_pipeline.py (runner analog to replicate exactly):
```python
from pathlib import Path
from src.ingest_rnaseq import ingest_rnaseq
from src.ingest_cnv import ingest_cnv
from src.ingest_methylation import ingest_methylation

OUT = Path("data/processed")

ingest_rnaseq(OUT, project_id="TCGA-BRCA")
ingest_cnv(OUT, project_id="TCGA-BRCA")
ingest_methylation(OUT, project_id="TCGA-BRCA")
```
No if __name__ == "__main__" guard. No argparse. Direct call.

From tests/test_ingest_rnaseq.py — test structure to replicate:
- Module-level docstring
- Imports: pathlib, tempfile, unittest (+ polars as pl for assertions)
- No mock.patch needed for merge tests (no S3)
- Deferred import inside each test method: from src.merge import ...
- TemporaryDirectory block: all file I/O + assertions inside the with block
- if __name__ == "__main__": unittest.main() at the end

Input Parquet schemas (for synthetic data creation):
  rna_seq.parquet:      patient_id (str), gene_id (str), fpkm_unstranded (float)
  cnv.parquet:          patient_id (str), chromosome (str e.g. "chr1"), start (int), end (int), copy_number (float)
  methylation.parquet:  patient_id (str), probe_id (str e.g. "cg00000001"), beta_value (float or null)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write tests/test_merge.py with four test classes</name>
  <files>tests/test_merge.py</files>

  <read_first>
    - /Users/aidancarlisle/Documents/cancer_genome_atlas_research/tests/test_ingest_rnaseq.py — exact module structure: docstring, imports, helper function, class layout, deferred import pattern, if __name__ guard
    - /Users/aidancarlisle/Documents/cancer_genome_atlas_research/tests/test_ingest_cnv.py — synthetic data helper pattern and assertion style
    - /Users/aidancarlisle/Documents/cancer_genome_atlas_research/.planning/phases/02-merge/02-PATTERNS.md — TestMergeAllCohorts examples, _make_data_dir helper, per-class key assertions table
    - /Users/aidancarlisle/Documents/cancer_genome_atlas_research/.planning/phases/02-merge/02-RESEARCH.md — Validation Architecture section, per-requirement test map
    - /Users/aidancarlisle/Documents/cancer_genome_atlas_research/src/merge.py — (after plan 02-01 executes) verify exact function signatures before writing tests
  </read_first>

  <action>
Create /Users/aidancarlisle/Documents/cancer_genome_atlas_research/tests/test_merge.py with the following structure:

MODULE DOCSTRING:
  """Tests for src/merge.py — pivot/join orchestrator for TCGA multi-omics merge."""

IMPORTS (module level, no mock needed):
  import pathlib
  import tempfile
  import unittest
  import polars as pl

MODULE-LEVEL HELPER — _make_data_dir:
  Signature: def _make_data_dir(tmp: pathlib.Path) -> pathlib.Path
  Purpose: Write minimal synthetic Parquet files for all 3 cohorts × 3 modalities into tmp/.
  Implementation:
    cohorts = ["TCGA-BRCA", "TCGA-LUAD", "TCGA-PRAD"]
    genes = [f"ENSG{i:011d}.1" for i in range(10)]      # 10 distinct genes with varied values to test variance
    probes = [f"cg{i:08d}" for i in range(10)]          # 10 distinct probes

    For each cohort (use distinct patient_id prefixes per cohort to avoid cross-cohort collision):
      patients = [f"TCGA-{cohort[5:9]}-P{i:03d}" for i in range(3)]  # e.g. TCGA-BRCA-P000
      d = tmp / cohort; d.mkdir(parents=True, exist_ok=True)

      rna_seq.parquet — 3 patients × 10 genes = 30 rows:
        rows = [{"patient_id": p, "gene_id": g, "fpkm_unstranded": float(i * 10 + j)}
                for i, p in enumerate(patients) for j, g in enumerate(genes)]
        pl.DataFrame(rows).write_parquet(d / "rna_seq.parquet")

      cnv.parquet — 3 patients × 2 segments each = 6 rows (use chr1 only):
        cnv_rows = [
            {"patient_id": p, "chromosome": "chr1", "start": 1000000, "end": 5000000, "copy_number": 0.1 * i},
            {"patient_id": p, "chromosome": "chr1", "start": 130000000, "end": 200000000, "copy_number": -0.1 * i},
        ]
        (Note: first segment midpoint 3000000 < centromere 123400000 → arm "1p"; second 165000000 > centromere → arm "1q")
        pl.DataFrame([row for p_rows in [cnv_rows_for_patient(p, i) for i, p in enumerate(patients)] for row in p_rows]).write_parquet(d / "cnv.parquet")

        Simplification: build the list inline. Each patient gets two rows:
          rows = []
          for i, p in enumerate(patients):
              rows.append({"patient_id": p, "chromosome": "chr1", "start": 1000000, "end": 5000000, "copy_number": 0.1 * i})
              rows.append({"patient_id": p, "chromosome": "chr1", "start": 130000000, "end": 200000000, "copy_number": -0.1 * i})
          pl.DataFrame(rows).write_parquet(d / "cnv.parquet")

      methylation.parquet — 3 patients × 10 probes = 30 rows:
        rows = [{"patient_id": p, "probe_id": pr, "beta_value": 0.1 * (i + j % 5)}
                for i, p in enumerate(patients) for j, pr in enumerate(probes)]
        pl.DataFrame(rows).write_parquet(d / "methylation.parquet")

    return tmp

CLASS 1 — TestRnaseqPivot (MERGE-01):
  Docstring: "MERGE-01: _pivot_rnaseq produces a wide matrix with patient_id × top gene columns."
  Methods:
    test_pivot_shape: Verify output shape is (n_patients, n_selected_genes + 1)
      - Write a single-cohort rna_seq.parquet with 3 patients × 10 genes
      - Call _pivot_rnaseq(path, top_genes=genes[:5]) (select 5 genes)
      - Assert result.shape == (3, 6)  # 3 patients, 5 genes + patient_id

    test_selected_genes_are_columns: Verify the correct gene IDs appear as columns
      - top_genes = genes[:3]
      - df = _pivot_rnaseq(path, top_genes)
      - for g in top_genes: self.assertIn(g, df.columns)

    test_excluded_genes_not_in_columns: Verify non-selected genes are absent
      - top_genes = genes[:3]; excluded = genes[3:]
      - df = _pivot_rnaseq(path, top_genes)
      - for g in excluded: self.assertNotIn(g, df.columns)

    Pattern for each test method:
      with tempfile.TemporaryDirectory() as tmp:
          d = pathlib.Path(tmp)
          genes = [f"ENSG{i:011d}.1" for i in range(10)]
          rows = [{"patient_id": f"P{i:03d}", "gene_id": g, "fpkm_unstranded": float(i+j)}
                  for i in range(3) for j, g in enumerate(genes)]
          rna_path = d / "rna_seq.parquet"
          pl.DataFrame(rows).write_parquet(rna_path)
          from src.merge import _pivot_rnaseq
          df = _pivot_rnaseq(rna_path, genes[:5])
          # assertions here (inside the with block — file still exists)

CLASS 2 — TestCnvPivot (MERGE-02):
  Docstring: "MERGE-02: _pivot_cnv produces chromosome-arm columns named 1p, 1q, etc."
  Methods:
    test_arm_column_names: Verify arm columns use NUMBERp/NUMBERq naming (not "p"/"q" alone)
      - Write cnv.parquet with segments on chr1 only (both p and q arms)
      - df = _pivot_cnv(path)
      - self.assertIn("1p", df.columns)
      - self.assertIn("1q", df.columns)
      - self.assertNotIn("p", df.columns)  # bare "p" would be wrong
      - self.assertNotIn("q", df.columns)  # bare "q" would be wrong

    test_midpoint_arm_assignment: Verify segments are assigned to correct arm
      - Segment start=1000000, end=5000000: midpoint=3000000 < chr1 centromere 123400000 → "1p"
      - Segment start=130000000, end=200000000: midpoint=165000000 > 123400000 → "1q"
      - patient "P000" gets one row per arm after groupby+mean
      - df = _pivot_cnv(path); row = df.filter(pl.col("patient_id") == "P000")
      - self.assertIsNotNone(row["1p"][0])
      - self.assertIsNotNone(row["1q"][0])

    test_noncanonical_chromosomes_filtered: chrM segments produce no arm columns
      - Write cnv.parquet with one row where chromosome="chrM"
      - df = _pivot_cnv(path)
      - assert no column named "Mp" or "Mq" appears
      - self.assertNotIn("Mp", df.columns)
      - self.assertNotIn("Mq", df.columns)

    test_one_row_per_patient: pivot produces exactly one row per patient_id
      - df = _pivot_cnv(path)  # 3 patients
      - self.assertEqual(df["patient_id"].n_unique(), len(df))

    Pattern for each test method:
      with tempfile.TemporaryDirectory() as tmp:
          cnv_path = pathlib.Path(tmp) / "cnv.parquet"
          rows = [...]
          pl.DataFrame(rows).write_parquet(cnv_path)
          from src.merge import _pivot_cnv
          df = _pivot_cnv(cnv_path)
          # assertions inside with block

CLASS 3 — TestMethylationPivot (MERGE-03):
  Docstring: "MERGE-03: _pivot_methylation produces wide matrix with selected probe columns; null beta handled."
  Methods:
    test_pivot_shape: shape is (n_patients, n_probes + 1)
      - 3 patients × 5 probes selected → (3, 6)

    test_null_beta_value_handled: null beta_value does not crash pivot
      - Write methylation.parquet with one null beta_value
      - rows include {"patient_id": "P000", "probe_id": "cg00000000", "beta_value": None}
      - _pivot_methylation(path, top_probes=["cg00000000"]) should not raise
      - The null cell should remain null (not coerced to 0.0) — check df["cg00000000"][0] is None

    test_excluded_probes_absent: probes not in top_probes do not appear as columns
      - top_probes = probes[:3]; excluded = probes[3:]
      - df = _pivot_methylation(path, top_probes)
      - for pr in excluded: self.assertNotIn(pr, df.columns)

    Pattern: same as RNA-seq pivot tests but with probe_id / beta_value schema.

CLASS 4 — TestMergeAllCohorts (MERGE-04):
  Docstring: "MERGE-04: merge_all_cohorts produces correct shape, schema, and inner-join semantics."
  Methods:
    test_output_file_exists:
      with tempfile.TemporaryDirectory() as tmp:
          data_dir = _make_data_dir(pathlib.Path(tmp))
          from src.merge import merge_all_cohorts
          out = merge_all_cohorts(data_dir, data_dir)
          self.assertTrue(out.exists())
          self.assertEqual(out.name, "merged_all_cohorts.parquet")

    test_one_row_per_patient:
      # All assertions INSIDE the with block — file is on disk inside TemporaryDirectory
      with tempfile.TemporaryDirectory() as tmp:
          data_dir = _make_data_dir(pathlib.Path(tmp))
          from src.merge import merge_all_cohorts
          out = merge_all_cohorts(data_dir, data_dir)
          df = pl.read_parquet(out)
          self.assertEqual(df["patient_id"].n_unique(), len(df))

    test_cohort_column_values:
      # cohort column has exactly {"BRCA", "LUAD", "PRAD"}
      with tempfile.TemporaryDirectory() as tmp:
          data_dir = _make_data_dir(pathlib.Path(tmp))
          from src.merge import merge_all_cohorts
          out = merge_all_cohorts(data_dir, data_dir)
          df = pl.read_parquet(out)
          self.assertIn("cohort", df.columns)
          self.assertEqual(set(df["cohort"].to_list()), {"BRCA", "LUAD", "PRAD"})

    test_inner_join_excludes_missing_patients:
      # A patient present in rna_seq but absent from cnv must not appear in output
      with tempfile.TemporaryDirectory() as tmp:
          data_dir = _make_data_dir(pathlib.Path(tmp))
          # Overwrite BRCA cnv.parquet with only 2 of 3 patients (drop third)
          brca_rna = pl.read_parquet(data_dir / "TCGA-BRCA" / "rna_seq.parquet")
          all_brca_patients = brca_rna["patient_id"].unique().to_list()
          excluded = all_brca_patients[-1]  # drop one patient from CNV
          brca_cnv_full = pl.read_parquet(data_dir / "TCGA-BRCA" / "cnv.parquet")
          brca_cnv_trimmed = brca_cnv_full.filter(pl.col("patient_id") != excluded)
          brca_cnv_trimmed.write_parquet(data_dir / "TCGA-BRCA" / "cnv.parquet")
          from src.merge import merge_all_cohorts
          out = merge_all_cohorts(data_dir, data_dir)
          df = pl.read_parquet(out)
          brca_patients_in_output = df.filter(pl.col("cohort") == "BRCA")["patient_id"].to_list()
          self.assertNotIn(excluded, brca_patients_in_output)

FOOTER:
  if __name__ == "__main__":
      unittest.main()

CRITICAL RULES for test_merge.py:
  1. ALL pl.read_parquet() calls MUST happen INSIDE the with tempfile.TemporaryDirectory() block
     (files are deleted when the block exits — reading after exit causes FileNotFoundError)
  2. Deferred imports: do `from src.merge import X` inside each test method, not at module level
  3. No unittest.mock.patch needed — merge.py reads local Parquet, not S3
  4. Each test method is self-contained — creates its own data and runs its own assertions
  </action>

  <verify>
    <automated>cd /Users/aidancarlisle/Documents/cancer_genome_atlas_research && python -m pytest tests/test_merge.py -x -q --tb=short 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `python -m pytest tests/test_merge.py -x -q` exits 0 (all tests pass)
    - `grep -n "class TestRnaseqPivot" tests/test_merge.py` returns one line
    - `grep -n "class TestCnvPivot" tests/test_merge.py` returns one line
    - `grep -n "class TestMethylationPivot" tests/test_merge.py` returns one line
    - `grep -n "class TestMergeAllCohorts" tests/test_merge.py` returns one line
    - `grep "from src.merge import" tests/test_merge.py | wc -l` returns at least 4 (one deferred import per class minimum)
    - `grep "TemporaryDirectory" tests/test_merge.py | wc -l` returns at least 4 (one per test method that touches files)
    - `grep "if __name__" tests/test_merge.py` returns exactly one line
    - `grep "mock\|patch\|Mock" tests/test_merge.py` returns zero lines (no mocking needed)
    - `python -m pytest tests/test_merge.py::TestRnaseqPivot -x -q` exits 0
    - `python -m pytest tests/test_merge.py::TestCnvPivot -x -q` exits 0
    - `python -m pytest tests/test_merge.py::TestMethylationPivot -x -q` exits 0
    - `python -m pytest tests/test_merge.py::TestMergeAllCohorts -x -q` exits 0
  </acceptance_criteria>

  <done>tests/test_merge.py exists with four test classes. python -m pytest tests/test_merge.py -x -q exits 0 with all tests passing.</done>
</task>

<task type="auto">
  <name>Task 2: Create run_merge.py runner</name>
  <files>run_merge.py</files>

  <read_first>
    - /Users/aidancarlisle/Documents/cancer_genome_atlas_research/run_pipeline.py — exact pattern to replicate (imports, Path constant, direct call, no argparse, no __main__ guard)
    - /Users/aidancarlisle/Documents/cancer_genome_atlas_research/.planning/phases/02-merge/02-PATTERNS.md — run_merge.py section (lines 229-259): shows analog is run_pipeline.py and provides exact template
    - /Users/aidancarlisle/Documents/cancer_genome_atlas_research/src/merge.py — confirm merge_all_cohorts signature: (data_dir: pathlib.Path, output_dir: pathlib.Path) -> pathlib.Path
  </read_first>

  <action>
Create /Users/aidancarlisle/Documents/cancer_genome_atlas_research/run_merge.py mirroring run_pipeline.py exactly:

EXACT CONTENT:
```python
from pathlib import Path
from src.merge import merge_all_cohorts

DATA_DIR = Path("data")
OUTPUT_DIR = Path("data")

out = merge_all_cohorts(DATA_DIR, OUTPUT_DIR)
print(f"Merged matrix written to {out}")
```

Rules:
  - No if __name__ == "__main__" guard (run_pipeline.py has none — follow same convention)
  - No argparse (not needed for course deliverable; paths are hardcoded per project decision)
  - DATA_DIR = Path("data") — Phase 1 outputs are at data/{cohort}/{modality}.parquet
  - OUTPUT_DIR = Path("data") — output will be data/merged_all_cohorts.parquet
  - print() after merge call so the user sees the output path when running the script directly
  - f-string in print() is fine here (this is not a logger call)
  </action>

  <verify>
    <automated>cd /Users/aidancarlisle/Documents/cancer_genome_atlas_research && python -c "import ast, pathlib; src=pathlib.Path('run_merge.py').read_text(); ast.parse(src); print('syntax ok')" && grep -q "merge_all_cohorts(DATA_DIR, OUTPUT_DIR)" run_merge.py && echo "call site ok"</automated>
  </verify>

  <acceptance_criteria>
    - `python -c "import ast, pathlib; ast.parse(pathlib.Path('run_merge.py').read_text()); print('ok')"` exits 0 (valid Python syntax)
    - `grep "from src.merge import merge_all_cohorts" run_merge.py` returns one line
    - `grep "DATA_DIR = Path" run_merge.py` returns one line
    - `grep "OUTPUT_DIR = Path" run_merge.py` returns one line
    - `grep "merge_all_cohorts(DATA_DIR, OUTPUT_DIR)" run_merge.py` returns one line
    - `grep "print" run_merge.py` returns one line (output path confirmation)
    - `grep "argparse\|ArgumentParser\|add_argument" run_merge.py` returns zero lines (no argparse)
    - `grep "__main__" run_merge.py` returns zero lines (no __main__ guard — mirrors run_pipeline.py)
  </acceptance_criteria>

  <done>run_merge.py exists at project root. Syntax is valid. grep checks pass. Pattern mirrors run_pipeline.py exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → test runner | Synthetic Parquet files written to tmpdir by test helper — fully trusted, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | tests/test_merge.py — TemporaryDirectory cleanup | accept | tmpdir cleaned by Python stdlib on exit; no sensitive TCGA data in synthetic test files |
| T-02-05 | Tampering | run_merge.py reading data/ on disk | accept | Same trust level as plan 02-01; local research pipeline, no adversarial input |
</threat_model>

<verification>
```bash
cd /Users/aidancarlisle/Documents/cancer_genome_atlas_research
# Run full test suite to confirm no regressions
python -m pytest tests/ -v --tb=short 2>&1 | tail -30
# Verify run_merge.py is syntactically valid
python -c "import ast, pathlib; ast.parse(pathlib.Path('run_merge.py').read_text()); print('run_merge.py syntax ok')"
```
</verification>

<success_criteria>
- python -m pytest tests/test_merge.py -x -q exits 0 (all four test classes passing)
- python -m pytest tests/ -v exits 0 (no regressions in Phase 1 test suite)
- run_merge.py exists, parses without error, mirrors run_pipeline.py structure
- All four MERGE-XX requirements are covered by at least one passing test
</success_criteria>

<output>
After completion, create /Users/aidancarlisle/Documents/cancer_genome_atlas_research/.planning/phases/02-merge/02-02-SUMMARY.md
</output>
